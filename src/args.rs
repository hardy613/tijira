// this file describes the arguments 
// tijira will use
#[derive(Debug)]
pub struct Args {
    pub user: String,
    pub password: String,
    pub host: String,
    pub identifier: String,
    pub ticket: String,
    pub api: String
}
